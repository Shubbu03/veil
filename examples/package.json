{
  "name": "veil-examples",
  "version": "0.1.0",
  "description": "Examples and tests for Veil private payments",
  "license": "MIT",
  "scripts": {
    "00-mint-token": "ts-node mint-test-token.ts",
    "01-init-vault": "ts-node 01-init-vault.ts",
    "02-deposit": "ts-node 02-deposit.ts",
    "03-create-schedule": "ts-node 03-create-schedule.ts",
    "04-register": "ts-node 04-register-with-coordinator.ts",
    "05-full-flow": "ts-node 05-full-flow.ts",
    "all": "yarn run 01-init-vault && yarn run 02-deposit && yarn run 03-create-schedule && yarn run 04-register"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.30.1",
    "@solana/web3.js": "^1.95.0",
    "@solana/spl-token": "^0.4.8"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "ts-node": "^10.9.2"
  }
}
